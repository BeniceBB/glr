<form method="post" action="{{ path('app_trip_delete', {'id': trip.id}) }}" onsubmit="return confirm('Weet je zeker dat je deze reis wilt verwijderen?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ trip.id) }}">
    <button class="btn btn-danger right">Verwijderen</button>
</form>
